# 隐私与安全

## 不应进入公开仓库的数据

UIS 账号/密码、Canvas token、浏览器 storage state/cookie、API key、SMTP 授权码、
SSH 密钥和连接配置、课程确认名单、Manifest、SQLite 数据库（包括加密备份）、
课件、录音、视频、转写、邮件内容以及真实运行日志，都不应提交。

`.gitignore` 是防误提交措施，不是保密机制。不要用 `git add -f` 强行添加运行文件。
对外分享报错前仍需人工检查：第三方库异常可能包含带签名的地址、请求细节或课程信息。
Canvas 日志内置脱敏，但不应假设所有组件与第三方依赖都能完整脱敏。

## 数据发送到哪里

- UIS、Canvas、iCourse 和 WebVPN：使用你的账号完成认证及有权限的课程读取。
- SenseVoice / Silero：本地 CPU 推理；首次启动从模型分发站下载权重。
- ModelScope：默认摘要服务，发送课程标题和转写内容。
- Gemini：配置 key 后优先尝试该服务，也会发送转写内容。
- SMTP：启用后发送课程笔记；数学公式邮件渲染可能请求第三方公式图片服务。

是否允许发送课程内容至第三方服务，需要使用者按学校、教师和服务商规定自行确认。
本仓库沿用运行版本的摘要调用方式，不把云端摘要描述成“全程本地处理”。

## 部署

- secrets / `.env` / 私有名单文件使用 `0600`，私有目录使用 `0700`。
- SQLite、映射与 token 只放在被忽略的私有运行目录；备份同样视作敏感信息。
- Compose 未对外发布任何端口，不提供公网 NAS 管理入口。
- 远程隧道、服务器地址、NAS 管理密码不属于本项目部署范围。
- Docker 组权限相当于很高的宿主机权限，只授予可信管理员。
- 不在公共 CI 中运行学校登录或真实课程同步。本仓库提供的 CI 模板仅使用模拟数据。
- 遇到泄露先吊销或轮换凭据，删除 Git 文件本身不能撤回已公开的历史。

请勿在公开 Issue、PR 或 Actions 日志中粘贴密码、完整日志、数据库或课程内容。
先提供不含敏感数据的复现步骤；安全问题应通过维护者可用的私下渠道沟通。
